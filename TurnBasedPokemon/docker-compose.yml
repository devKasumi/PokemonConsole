version: '3.8'

services:
  mysql-db:
    image: mysql:8.0
    container_name: pokemon-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: yatokami11235813@
      MYSQL_DATABASE: pokemondb
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: